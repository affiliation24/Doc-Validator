# Зависимости Doc Validator.
# Раньше здесь лежал pip freeze всего окружения (~300 пакетов: torch, jupyter,
# aiogram, kaggle). Осталось только то, что действительно импортирует проект.

# API
fastapi==0.135.1
uvicorn==0.41.0
pydantic==2.9.2
python-multipart==0.0.22

# LLM и конфигурация
groq==1.1.1
python-dotenv==1.1.1

# RAG: векторная база и эмбеддинги
chromadb==1.5.5
sentence-transformers==5.3.0

# Извлечение текста из PDF и сканов
pdfplumber==0.11.9
pytesseract==0.3.13
pillow==11.1.0

# Тесты
pytest==8.3.4
requests==2.32.3
